---
description: Show btc-collector manifest + registry progress
---

Run `python main.py status` from the btc-collector project root and summarize the output. After printing the raw output, give a one-paragraph summary covering:
- How many months are pending / in_progress / done / failed in the manifest
- How many symbols are pending / done / empty / not_listed / failed in the registry
- Anything that looks unexpected (stale in_progress, unusual failed counts)

Do NOT modify any data. This is read-only.
